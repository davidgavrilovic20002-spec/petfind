{
  "name": "PetFind",
  "short_name": "PetFind",
  "description": "The QR pet tag that always works — scan, call the owner, find the nearest vet.",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#EDF2F1",
  "theme_color": "#0F766E",
  "icons": [
    { "src": "assets/img/favicon.svg", "sizes": "any", "type": "image/svg+xml", "purpose": "any maskable" }
  ]
}
